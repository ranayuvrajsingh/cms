import * as ROUTES from '../../utils/constants';
import { Masters } from '../../screens';

const MasterRoutes = [
  {
    path: ROUTES.MASTERS,
    component: Masters,
    exact: true,
  },
];

export default MasterRoutes;
